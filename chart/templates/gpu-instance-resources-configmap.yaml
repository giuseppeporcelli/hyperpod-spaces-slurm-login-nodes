apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hyperpod-spaces-user-webhook.fullname" . }}-gpu-instance-resources
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "hyperpod-spaces-user-webhook.labels" . | nindent 4 }}
data:
  config.json: |
    {{- .Values.gpuInstanceResources | toPrettyJson | nindent 4 }}
