apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hyperpod-spaces-user-webhook.fullname" . }}-cpu-instance-types
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "hyperpod-spaces-user-webhook.labels" . | nindent 4 }}
data:
  config.json: |
    {{- .Values.cpuInstanceTypes | toPrettyJson | nindent 4 }}
