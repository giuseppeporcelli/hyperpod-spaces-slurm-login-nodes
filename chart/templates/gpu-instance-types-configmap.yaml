apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hyperpod-spaces-user-webhook.fullname" . }}-gpu-instance-types
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "hyperpod-spaces-user-webhook.labels" . | nindent 4 }}
data:
  instance-types.json: |
    {{ .Values.gpuInstanceTypes | toJson }}
